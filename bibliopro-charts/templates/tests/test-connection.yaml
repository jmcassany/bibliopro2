apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bibliopro-charts.fullname" . }}-test-connection"
  labels:
    {{- include "bibliopro-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bibliopro-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
